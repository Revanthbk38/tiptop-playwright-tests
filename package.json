{
  "name": "tiptop-playwright-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test --reporter=html",
    "show-report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.38.0"
  }
}
